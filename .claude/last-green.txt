# VoiceReach last-green test baseline
# Updated: 2026-08-08 | Node v20.20.2 | branch feat/phase-3
# Totals: server=452, web=199, combined=651

### SERVER (/server, vitest, 452 tests) ###
tests/app.test.js > GET /api/gmail/callback > 400s when Google reports an error
tests/app.test.js > GET /api/gmail/callback > is public but 400s without a code and state
tests/app.test.js > GET /api/health > is public and returns ok
tests/app.test.js > GET /api/health > sets helmet security headers
tests/app.test.js > GET /api/me > 401s with an invalid token
tests/app.test.js > GET /api/me > 401s without a token
tests/app.test.js > GET /api/me > returns the authenticated user
tests/app.test.js > POST /api/gmail/connect > 401s without a token
tests/app.test.js > app hardening > allows the configured APP_URL origin
tests/app.test.js > app hardening > does not send CORS headers to a disallowed origin
tests/app.test.js > app hardening > returns 400 {error} for malformed JSON bodies
tests/app.test.js > app hardening > returns JSON {error} for unknown routes
tests/app.test.js > voice routes > 401s on GET /api/voice without a token
tests/app.test.js > voice routes > 401s on POST /api/voice/generate without a token
tests/auth.test.js > extractBearerToken > is case-insensitive on the scheme
tests/auth.test.js > extractBearerToken > returns null for a non-bearer scheme
tests/auth.test.js > extractBearerToken > returns null when the header is missing
tests/auth.test.js > extractBearerToken > returns null when the scheme has no token
tests/auth.test.js > extractBearerToken > returns null when there are extra segments
tests/auth.test.js > extractBearerToken > returns the token for a well-formed header
tests/auth.test.js > extractBearerToken > tolerates a request with no headers object
tests/auth.test.js > requireAuth > 401s when Supabase rejects the token
tests/auth.test.js > requireAuth > 401s when Supabase returns no user
tests/auth.test.js > requireAuth > 401s when no Authorization header is present
tests/auth.test.js > requireAuth > 500s without leaking details when verification throws
tests/auth.test.js > requireAuth > attaches req.user and calls next on a valid token
tests/campaignRoutes.test.js > GET /api/campaigns > lists the caller's campaigns with counts
tests/campaignRoutes.test.js > GET /api/campaigns/:id > 400s on an id that is not a campaign id, without touching the service
tests/campaignRoutes.test.js > GET /api/campaigns/:id > 404s when the campaign is not the caller's
tests/campaignRoutes.test.js > GET /api/campaigns/:id > returns the detail with its leads
tests/campaignRoutes.test.js > PATCH /api/campaigns/:id > 400s on a malformed campaign id
tests/campaignRoutes.test.js > PATCH /api/campaigns/:id > forwards only the editable fields
tests/campaignRoutes.test.js > POST /api/campaigns > creates the campaign for the authenticated user
tests/campaignRoutes.test.js > POST /api/campaigns > ignores a user_id in the body — the owner is the token, always
tests/campaignRoutes.test.js > POST /api/campaigns > returns the service status and message for invalid input
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 400s above the row cap and never reaches the service
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 400s cleanly on an empty rows array
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 400s on an id that is not a campaign id
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 400s when rows is missing or not an array, rather than 500ing
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 401s without a token, without reaching the service
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > 404s when the campaign is not the caller's
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > forwards the rows for the authenticated user and returns the summary
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > logs ids and a count only — never a row
tests/campaignRoutes.test.js > POST /api/campaigns/:id/leads > logs no row content when the upload fails either
tests/campaignRoutes.test.js > campaign routes — authentication > 401s on every campaign route without a token
tests/campaigns.test.js > campaign logging > logs ids and counts only — never a name, subject or template body
tests/campaigns.test.js > createCampaign > creates a voice campaign with a null template body
tests/campaigns.test.js > createCampaign > rejects a template with no {{personalized}} section
tests/campaigns.test.js > createCampaign > rejects an empty or whitespace-only name
tests/campaigns.test.js > createCampaign > rejects an over-long name
tests/campaigns.test.js > createCampaign > rejects any mode that is not exactly 'voice' or 'template'
tests/campaigns.test.js > createCampaign > rejects template mode with no template body at all
tests/campaigns.test.js > createCampaign > requires a userId
tests/campaigns.test.js > createCampaign > stores the template verbatim when it carries a {{personalized}} section
tests/campaigns.test.js > getCampaign > 404s on an unknown campaign id
tests/campaigns.test.js > getCampaign > 404s on another user's campaign, and filters both reads on user_id
tests/campaigns.test.js > getCampaign > returns the campaign with its leads and counts
tests/campaigns.test.js > getCampaign > scopes the lead read to the owner as well as the campaign
tests/campaigns.test.js > listCampaigns > filters on user_id, in one query, newest first
tests/campaigns.test.js > listCampaigns > returns sent and replied counts, and zero for a campaign with no leads
tests/campaigns.test.js > updateCampaign > 400s when a patch carries nothing editable
tests/campaigns.test.js > updateCampaign > 404s on another user's campaign and writes nothing
tests/campaigns.test.js > updateCampaign > cannot change status, user_id, mode or id
tests/campaigns.test.js > updateCampaign > carries user_id on the write itself, not just the ownership check
tests/campaigns.test.js > updateCampaign > clears the subject template when it is emptied
tests/campaigns.test.js > updateCampaign > rejects a template body on a voice campaign
tests/campaigns.test.js > updateCampaign > rejects a template body that loses its {{personalized}} section
tests/campaigns.test.js > updateCampaign > updates the name and subject template
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — auth > 401s when the token does not resolve to a user
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — auth > 401s without an Authorization header, and never touches Gmail
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > counts the corpus the voice builder would receive, skipping empty bodies
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > flags the same rows the review UI would flag
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > logs counts only, never content
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > returns exactly the five counts and nothing else
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > returns no body, subject or recipient text at any level
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — counts only > sets Cache-Control: no-store
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — failures > 400s with {error} when Gmail is not connected
tests/corpusSummary.test.js > GET /api/voice/corpus-summary — failures > 500s with a generic {error} when the profile read fails
tests/crypto.test.js > encrypt / decrypt > does not echo plaintext in the failure message
tests/crypto.test.js > encrypt / decrypt > never stores the plaintext in the blob
tests/crypto.test.js > encrypt / decrypt > prefixes the blob with the v1 version byte
tests/crypto.test.js > encrypt / decrypt > produces a different ciphertext each time for identical input (random IV)
tests/crypto.test.js > encrypt / decrypt > rejects a blob encrypted under a different key
tests/crypto.test.js > encrypt / decrypt > rejects a tampered blob
tests/crypto.test.js > encrypt / decrypt > rejects a truncated blob
tests/crypto.test.js > encrypt / decrypt > rejects an unknown version byte
tests/crypto.test.js > encrypt / decrypt > rejects non-string inputs
tests/crypto.test.js > encrypt / decrypt > round-trips a value
tests/crypto.test.js > encrypt / decrypt > round-trips an empty string
tests/crypto.test.js > encrypt / decrypt > round-trips unicode and multi-line JSON
tests/crypto.test.js > encrypt / decrypt > throws a clear error when TOKEN_ENC_KEY is missing
tests/crypto.test.js > encrypt / decrypt > throws a clear error when TOKEN_ENC_KEY is the wrong length
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > flags html markup that survived into the extracted text
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > flags nothing removed for a clean body
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > flags the quoted reply and signature it stripped
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > logs counts only, never bodies
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > omits raw by default and includes it with ?includeRaw=1
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > returns the summary counts for the corpus the voice builder would receive
tests/devInspect.test.js > GET /api/dev/ingest-preview — corpus > sets Cache-Control: no-store so the corpus is never cached
tests/devInspect.test.js > GET /api/dev/ingest-preview — failures > 400s with {error} when Gmail is not connected
tests/devInspect.test.js > GET /api/dev/ingest-preview — failures > 500s with a generic {error} when the profile read fails
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 401s without a token when enabled
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='0' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='1' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='TRUE' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='false' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s for ENABLE_DEV_ROUTES='yes' (exact 'true' match only)
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s in production even when ENABLE_DEV_ROUTES is true
tests/devInspect.test.js > GET /api/dev/ingest-preview — gating > 404s when ENABLE_DEV_ROUTES is unset
tests/generate.test.js > findBannedPhrases > allows "leverage" and "delve" when the user writes them themselves
tests/generate.test.js > findBannedPhrases > bans "quick question" as a subject but not inside the body
tests/generate.test.js > findBannedPhrases > flags every banned phrase in the subject or body
tests/generate.test.js > generateEmail > appends no unsubscribe footer: the body is exactly what the model wrote
tests/generate.test.js > generateEmail > decrypts the exemplars into the prompt and never logs them
tests/generate.test.js > generateEmail > does not regenerate when the fidelity score clears the bar
tests/generate.test.js > generateEmail > never echoes model output when the response is not JSON
tests/generate.test.js > generateEmail > regenerates exactly once when a banned phrase is used
tests/generate.test.js > generateEmail > regenerates once on a low fidelity score and returns the score rather than throwing
tests/generate.test.js > generateEmail > rejects "quick question" as a subject and regenerates once
tests/generate.test.js > generateEmail > returns a subject and body, parsing a fenced ```json response
tests/generate.test.js > generateEmail > still generates when the profile has no exemplars stored
tests/generate.test.js > generateEmail > validates its input before calling the model
tests/generate.test.js > generateEmail — signing off as the user > does not block or retry when signoff_styles is missing, empty or malformed
tests/generate.test.js > generateEmail — signing off as the user > does not retry a draft that already signs off
tests/generate.test.js > generateEmail — signing off as the user > names the user's own closings in the draft prompt
tests/generate.test.js > generateEmail — signing off as the user > regenerates exactly once when the draft does not sign off
tests/generate.test.js > generateEmail — signing off as the user > treats a closing with no name as unsigned
tests/gmail.test.js > buildAuthUrl > asks for offline access with a forced consent prompt
tests/gmail.test.js > buildAuthUrl > carries the user id in an opaque state param
tests/gmail.test.js > buildAuthUrl > rejects a state that was not issued by us
tests/gmail.test.js > buildAuthUrl > requests gmail.readonly and gmail.send, never gmail.modify
tests/gmail.test.js > cleanEmailBody > collapses runs of blank lines
tests/gmail.test.js > cleanEmailBody > returns an empty string for empty input
tests/gmail.test.js > cleanEmailBody > strips signature and quoted reply together
tests/gmail.test.js > extractPlainTextBody > falls back to stripped html when there is no text/plain part
tests/gmail.test.js > extractPlainTextBody > finds text/plain inside nested multipart parts
tests/gmail.test.js > extractPlainTextBody > normalises CRLF line endings
tests/gmail.test.js > extractPlainTextBody > returns an empty string for a missing or bodyless payload
tests/gmail.test.js > fetchSentEmails > appends an after: filter when one is supplied
tests/gmail.test.js > fetchSentEmails > caps at the 100 most recent sent emails
tests/gmail.test.js > fetchSentEmails > drops messages whose cleaned body is empty
tests/gmail.test.js > fetchSentEmails > logs counts only, never bodies
tests/gmail.test.js > fetchSentEmails > queries in:sent and returns an empty list when the mailbox is empty
tests/gmail.test.js > fetchSentEmails > uses whatever exists when there are fewer than 100
tests/gmail.test.js > getAuthedClient > decrypts the stored token in memory and sets it on the client
tests/gmail.test.js > getAuthedClient > rejects when Gmail is not connected
tests/gmail.test.js > handleCallback > never leaks the authorization code when the exchange fails
tests/gmail.test.js > handleCallback > rejects a missing code before calling Google
tests/gmail.test.js > handleCallback > rejects an invalid state before calling Google
tests/gmail.test.js > handleCallback > rejects when Google returns no refresh token
tests/gmail.test.js > handleCallback > stores the refresh token encrypted and marks the profile connected
tests/gmail.test.js > stripQuotedReplies > drops an -----Original Message----- block
tests/gmail.test.js > stripQuotedReplies > drops everything from an 'On ... wrote:' line onward
tests/gmail.test.js > stripQuotedReplies > drops lines starting with '>'
tests/gmail.test.js > stripQuotedReplies > handles a wrapped 'On ... wrote:' header
tests/gmail.test.js > stripQuotedReplies > leaves an unquoted body untouched
tests/gmail.test.js > stripSignature > drops everything after a '--' delimiter line
tests/gmail.test.js > stripSignature > drops trailing lines that look like a signature (phone / url)
tests/gmail.test.js > stripSignature > keeps a body with no signature at all
tests/gmailParse-signature.test.js > cleanEmailBody — composition > leaves an email with no signature unchanged
tests/gmailParse-signature.test.js > cleanEmailBody — composition > strips the quoted reply first, then the signature block under the closing
tests/gmailParse-signature.test.js > stripSignature — a domain inside prose is never a signature > keeps a labelled domain line
tests/gmailParse-signature.test.js > stripSignature — a domain inside prose is never a signature > keeps a one-line body that mentions a domain
tests/gmailParse-signature.test.js > stripSignature — a domain inside prose is never a signature > keeps a short sentence that ends in a domain
tests/gmailParse-signature.test.js > stripSignature — bare domains > handles a multi-part tld
tests/gmailParse-signature.test.js > stripSignature — bare domains > treats a bare domain on its own line as signature material
tests/gmailParse-signature.test.js > stripSignature — bare domains > treats a short bare-domain tld like .io as signature material
tests/gmailParse-signature.test.js > stripSignature — existing paths still work > drops everything after a '--' delimiter line
tests/gmailParse-signature.test.js > stripSignature — existing paths still work > keeps a body with no signature at all
tests/gmailParse-signature.test.js > stripSignature — existing paths still work > lets the '--' delimiter win over the closing-line path
tests/gmailParse-signature.test.js > stripSignature — existing paths still work > still drops trailing phone and schemed-url lines with no closing present
tests/gmailParse-signature.test.js > stripSignature — never deletes the whole body > keeps a body that is nothing but a '--' delimiter block
tests/gmailParse-signature.test.js > stripSignature — never deletes the whole body > keeps a body that is nothing but a bare domain
tests/gmailParse-signature.test.js > stripSignature — never deletes the whole body > keeps a body that is nothing but signature-shaped lines
tests/gmailParse-signature.test.js > stripSignature — never deletes the whole body > still returns an empty string for a blank body
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat a decimal number as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat a sentence-ending period as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat prose abbreviations as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "10.5" as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "U.S." as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "e.g." as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "etc." as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "friday." as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "i.e." as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > does not treat the standalone token "know.Thanks" as a bare domain
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a domain mentioned inside a prose sentence alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a short line containing e.g. tuesday alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a short line containing etc. tuesday alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a short line containing i.e. tomorrow alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a short line containing ok, e.g. friday alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (abbreviations and numbers) > leaves a short line containing the U.S. team alone
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (closing-word false positives) > does not strip a trailing block longer than 4 lines
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (closing-word false positives) > does not treat a closing word followed by lowercase prose as a closing
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (closing-word false positives) > does not truncate when "Thanks" opens a sentence mid-body
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (closing-word false positives) > does not truncate when a real closing is followed by real sentences
tests/gmailParse-signature.test.js > stripSignature — over-strip guards (closing-word false positives) > never strips a closing that is the last line
tests/gmailParse-signature.test.js > stripSignature — postscripts are prose, never signature > keeps a P.S. sitting below the sign-off
tests/gmailParse-signature.test.js > stripSignature — postscripts are prose, never signature > keeps a trailing "PS one more thing" line
tests/gmailParse-signature.test.js > stripSignature — postscripts are prose, never signature > keeps a trailing "PS: one more thing" line
tests/gmailParse-signature.test.js > stripSignature — postscripts are prose, never signature > keeps a trailing "p.s. one more thing" line
tests/gmailParse-signature.test.js > stripSignature — postscripts are prose, never signature > refuses the cut entirely when a P.S. follows a real signature block
tests/gmailParse-signature.test.js > stripSignature — short prose under a closing is not a signature block > keeps a short capitalised sentence under a one-word closing
tests/gmailParse-signature.test.js > stripSignature — short prose under a closing is not a signature block > keeps several short punchy sentences under a closing-shaped opener
tests/gmailParse-signature.test.js > stripSignature — signature block under a closing line > does not cut a lone bare name under a closing — shape alone is not evidence
tests/gmailParse-signature.test.js > stripSignature — signature block under a closing line > drops a block ending in a bare name when a title line sits above it
tests/gmailParse-signature.test.js > stripSignature — signature block under a closing line > drops the name/title/company/url block but KEEPS the closing line
tests/gmailParse-signature.test.js > stripSignature — signature block under a closing line > handles a multi-word closing plus a title and phone block
tests/httpError.test.js > httpError > builds an Error carrying a status and an expose flag
tests/httpError.test.js > safeMessage > hides messages from plain errors
tests/httpError.test.js > safeMessage > hides messages on 5xx even when they are ours
tests/httpError.test.js > safeMessage > hides third-party SDK messages that happen to carry a status
tests/httpError.test.js > safeMessage > returns our own 4xx messages
tests/leads.test.js > addLeads — address verification is advisory > carries a DNS timeout through as 'unknown', never as invalid
tests/leads.test.js > addLeads — address verification is advisory > does not let the representative address lend its role flag to its neighbours
tests/leads.test.js > addLeads — address verification is advisory > flags a domain with no mail server but still inserts it
tests/leads.test.js > addLeads — address verification is advisory > flags a role inbox per row even though its domain was checked once
tests/leads.test.js > addLeads — address verification is advisory > resolves once per unique domain, not once per row
tests/leads.test.js > addLeads — address verification is advisory > still completes the upload when verification itself falls over
tests/leads.test.js > addLeads — duplicates are a no-op, not an error > compares case-insensitively and ignores surrounding whitespace
tests/leads.test.js > addLeads — duplicates are a no-op, not an error > does not treat a lead on another campaign as a duplicate
tests/leads.test.js > addLeads — duplicates are a no-op, not an error > skips an address already on the campaign
tests/leads.test.js > addLeads — duplicates are a no-op, not an error > skips an address listed twice in the same upload, inserting it once
tests/leads.test.js > addLeads — one bad row is that row s problem > rejects an address past the RFC 5321 length without touching the insert
tests/leads.test.js > addLeads — one bad row is that row s problem > rejects the malformed rows and still inserts the good ones
tests/leads.test.js > addLeads — owner scoping > 404s on a campaign that does not exist
tests/leads.test.js > addLeads — owner scoping > 404s on another user's campaign and writes nothing
tests/leads.test.js > addLeads — owner scoping > stamps every inserted row with the caller's id and the campaign from the URL
tests/leads.test.js > addLeads — the envelope > 400s on an empty rows array rather than 500ing on it
tests/leads.test.js > addLeads — the envelope > 400s when rows is not an array at all
tests/leads.test.js > addLeads — the envelope > 400s without a userId or a campaign id
tests/leads.test.js > addLeads — the envelope > accepts a request sitting exactly on the cap
tests/leads.test.js > addLeads — the envelope > refuses more rows than the cap and inserts nothing
tests/leads.test.js > addLeads — the happy path > inserts valid rows as 'pending'
tests/leads.test.js > addLeads — the happy path > trims every value before it is stored
tests/leads.test.js > addLeads — what a row is not allowed to carry > ignores a client-supplied status, user_id and campaign_id
tests/leads.test.js > addLeads — what a row is not allowed to carry > never lets an unknown column reach the insert
tests/leads.test.js > checkLeadAddresses > gives every row on a dead domain the same verdict from one lookup
tests/leads.test.js > checkLeadAddresses > returns 'ok' with no reasons for a healthy address
tests/leads.test.js > checkLeadAddresses > returns an empty list for an empty input without asking the resolver
tests/leads.test.js > lead upload logging > logs ids and counts only — never an address, name, company or title
tests/leads.test.js > lead upload logging > logs nothing at all when the campaign is not the caller s
tests/logger.test.js > logger > sends errors to console.error
tests/logger.test.js > logger > writes a single structured JSON line without disallowed fields
tests/logger.test.js > sanitizeMeta > drops fields that are not allowlisted (e.g. email bodies)
tests/logger.test.js > sanitizeMeta > keeps allowlisted fields
tests/logger.test.js > sanitizeMeta > redacts non-scalar values
tests/logger.test.js > sanitizeMeta > redacts token-shaped values even on allowlisted fields
tests/logger.test.js > sanitizeMeta > returns an empty object for missing or non-object meta
tests/logger.test.js > sanitizeMeta > truncates long strings
tests/recipientCheck.test.js > verifyRecipient — MX > lowercases the domain before resolving
tests/recipientCheck.test.js > verifyRecipient — MX > reads ENOTFOUND as a domain that provably cannot receive mail
tests/recipientCheck.test.js > verifyRecipient — MX > reads NXDOMAIN the same way
tests/recipientCheck.test.js > verifyRecipient — MX > reads RFC 7505 null MX (a lone ".") as no_mx
tests/recipientCheck.test.js > verifyRecipient — MX > reads an empty MX record set as no_mx
tests/recipientCheck.test.js > verifyRecipient — MX > returns ok when the domain has MX records and nothing else is flagged
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > does not cache a non-list answer either
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > does not cache a timeout — the next lookup gets a clean answer
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > keeps the default timeout bounded and short
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > resolves to unknown for an error carrying no code at all
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > resolves to unknown when the resolver answers with something that is not a record list
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > resolves to unknown, NOT invalid, for any unexpected DNS error
tests/recipientCheck.test.js > verifyRecipient — a failed lookup is never a bad address > resolves to unknown, NOT invalid, when the lookup times out
tests/recipientCheck.test.js > verifyRecipient — quality flags > ignores a plus tag when matching a role address
tests/recipientCheck.test.js > verifyRecipient — quality flags > keeps the role and disposable lists non-empty and extendable
tests/recipientCheck.test.js > verifyRecipient — quality flags > leaves an ordinary local part alone
tests/recipientCheck.test.js > verifyRecipient — quality flags > reports both codes when an address is a role account on a disposable domain
tests/recipientCheck.test.js > verifyRecipient — quality flags > warns on a disposable domain
tests/recipientCheck.test.js > verifyRecipient — quality flags > warns on a role address that sits on a domain with MX
tests/recipientCheck.test.js > verifyRecipient — syntax > flags a malformed address without ever asking DNS
tests/recipientCheck.test.js > verifyRecipient — syntax > treats a non-string input as malformed rather than throwing
tests/recipientCheck.test.js > verifyRecipient — the MX cache > caches a no_mx answer too
tests/recipientCheck.test.js > verifyRecipient — the MX cache > does not re-query DNS for a domain it has already resolved
tests/recipientCheck.test.js > verifyRecipient — the MX cache > queries again for a different domain
tests/recipientCheck.test.js > verifyRecipient — the MX cache > re-queries after the cache is reset
tests/recipientCheck.test.js > verifyRecipient — the MX cache > shares one entry between two different addresses on the same domain
tests/recipientCheck.test.js > verifyRecipient — the MX cache > still applies per-address flags on a cache hit
tests/recipientCheck.test.js > verifyRecipient — the result shape > returns stable machine codes, never prose
tests/send.test.js > POST /api/send — the confirmation gate > 400s for other truthy stand-ins (1, "yes"), and never calls Gmail send
tests/send.test.js > POST /api/send — the confirmation gate > 400s when confirmed is false, and never calls Gmail send
tests/send.test.js > POST /api/send — the confirmation gate > 400s when confirmed is omitted, and never calls Gmail send
tests/send.test.js > POST /api/send — the confirmation gate > 400s when confirmed is the string 'true' — strict boolean only
tests/send.test.js > POST /api/send — the confirmation gate > 400s with {error} for a missing recipient, subject or body
tests/send.test.js > POST /api/send — the confirmation gate > 401s without a token
tests/send.test.js > POST /api/send — the confirmation gate > sends and returns the gmail ids when confirmed is exactly true
tests/send.test.js > POST /api/send — the confirmation gate > sends exactly the subject and body it was given, unmodified
tests/send.test.js > POST /api/send — the recovery action > adds nothing to a successful send
tests/send.test.js > POST /api/send — the recovery action > does NOT name it on a rate-limit 403, and still does not disconnect
tests/send.test.js > POST /api/send — the recovery action > does NOT name it on an unrecognised failure
tests/send.test.js > POST /api/send — the recovery action > names reconnect_gmail when the grant was revoked
tests/send.test.js > POST /api/send — the recovery action > names reconnect_gmail when the token predates a scope we now need
tests/send.test.js > POST /api/send/generate > 400s without a recipient or a goal, and never drafts
tests/send.test.js > POST /api/send/generate > 401s without a token
tests/send.test.js > POST /api/send/generate > returns the draft and NEVER calls gmail.users.messages.send
tests/send.test.js > POST /api/send/verify-recipient > 400s on a missing or malformed body, and never runs the check
tests/send.test.js > POST /api/send/verify-recipient > 401s without a token, and never runs the check
tests/send.test.js > POST /api/send/verify-recipient > cannot send: gmail.users.messages.send is never reached
tests/send.test.js > POST /api/send/verify-recipient > keeps the recipient address — and its domain — out of every log line
tests/send.test.js > POST /api/send/verify-recipient > marks the response no-store — a per-recipient verdict is not cacheable
tests/send.test.js > POST /api/send/verify-recipient > persists nothing — no table is touched on this path
tests/send.test.js > POST /api/send/verify-recipient > returns the verification result for a valid body
tests/send.test.js > buildMimeMessage > RFC 2047 encodes a non-ASCII subject
tests/send.test.js > buildMimeMessage > leaves a plain ASCII subject unencoded
tests/send.test.js > buildMimeMessage > rejects a message with a missing field
tests/send.test.js > buildMimeMessage > strips CR/LF out of header values so headers cannot be injected
tests/send.test.js > buildMimeMessage > strips CR/LF out of the recipient header so To cannot inject either
tests/send.test.js > buildMimeMessage > writes the required RFC 2822 headers followed by the body
tests/send.test.js > classifyGmailFailure > classifies malformed error objects without throwing
tests/send.test.js > classifyGmailFailure > falls back to the message text only for narrow, specific wording
tests/send.test.js > classifyGmailFailure > leaves non-4xx statuses alone
tests/send.test.js > classifyGmailFailure > reads 401 as a revoked grant
tests/send.test.js > classifyGmailFailure > reads ACCESS_TOKEN_SCOPE_INSUFFICIENT out of the nested response shape
tests/send.test.js > classifyGmailFailure > reads a 403 insufficientPermissions reason as a scope problem
tests/send.test.js > classifyGmailFailure > reads every rate-limit reason as rate limiting, not revocation
tests/send.test.js > classifyGmailFailure > treats an unrecognised or absent 403 reason as OTHER
tests/send.test.js > send logging > never logs the recipient, subject or body
tests/send.test.js > sendEmail > base64url-encodes the MIME message and calls gmail.users.messages.send
tests/send.test.js > sendEmail > never echoes Gmail's own error message
tests/send.test.js > sendEmail > reads the sender address from gmail.users.getProfile
tests/send.test.js > sendEmail > rejects an implausible recipient address with 400 before calling Gmail
tests/send.test.js > sendEmail > sets gmail_connected=false when Gmail answers 401 (auth revoked)
tests/send.test.js > sendEmail — responding to a Gmail 403 > asks for re-approval — not "revoked" — when the token predates a scope
tests/send.test.js > sendEmail — responding to a Gmail 403 > does NOT disconnect on a 403 carrying no reason at all
tests/send.test.js > sendEmail — responding to a Gmail 403 > does NOT disconnect on a 403 with an unrecognised reason
tests/send.test.js > sendEmail — responding to a Gmail 403 > does NOT disconnect on a rate-limit 403, and returns a retryable error
tests/send.test.js > sendEmail — responding to a Gmail 403 > keeps Gmail's raw error text out of both the logs and the thrown message
tests/sendFollowUp.test.js > POST /api/send — a follow-up is still gated > 400s a follow-up with confirmed omitted, and never calls Gmail send
tests/sendFollowUp.test.js > POST /api/send — a follow-up is still gated > 400s a follow-up with confirmed:false or the string 'true'
tests/sendFollowUp.test.js > POST /api/send — a follow-up is still gated > 400s an implausible threadId or inReplyTo with {error} JSON
tests/sendFollowUp.test.js > POST /api/send — a follow-up is still gated > 404s a follow-up on a thread the user did not send from Unique
tests/sendFollowUp.test.js > POST /api/send — a follow-up is still gated > sends the follow-up when confirmed is exactly true
tests/sendFollowUp.test.js > follow-up logging > never logs the recipient or subject when the send_log write fails
tests/sendFollowUp.test.js > follow-up logging > never logs the recipient, subject or body
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > 404s a threadId that is not in this user's send_log, before touching Gmail
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > does not double-prefix a subject that already starts with Re:
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > leaves a first send untouched — no Re:, no threading headers, no threadId
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > prefixes the subject with "Re: " exactly once
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > records the follow-up in send_log like any other send
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > sets In-Reply-To and References and passes the threadId to Gmail
tests/sendFollowUp.test.js > sendEmail — follow-up in an existing thread > wraps a bare Message-ID in angle brackets
tests/sendFollowUp.test.js > sendEmail — recording what we sent > does NOT fail the send when the send_log write rejects
tests/sendFollowUp.test.js > sendEmail — recording what we sent > does NOT fail the send when the send_log write returns an error
tests/sendFollowUp.test.js > sendEmail — recording what we sent > does not change the HTTP response when the send_log write fails
tests/sendFollowUp.test.js > sendEmail — recording what we sent > writes the Gmail ids to send_log after a successful send
tests/sendFollowUp.test.js > withReplyPrefix > adds "Re: " once and never twice
tests/signoff.test.js > findMissingSignoff > accepts a closing that already carries the name
tests/signoff.test.js > findMissingSignoff > accepts a one-word closing that stands on its own
tests/signoff.test.js > findMissingSignoff > carries no email content in the violation it returns
tests/signoff.test.js > findMissingSignoff > does not block on a malformed draft
tests/signoff.test.js > findMissingSignoff > does not block when there is nothing to match against
tests/signoff.test.js > findMissingSignoff > flags a bare closing with no name under it
tests/signoff.test.js > findMissingSignoff > flags a body that just stops, with no closing at all
tests/signoff.test.js > findMissingSignoff > ignores the appended unsubscribe line when reading the tail
tests/signoff.test.js > findMissingSignoff > matches case-insensitively
tests/signoff.test.js > findMissingSignoff > passes a body that closes and signs with a name
tests/signoff.test.js > findMissingSignoff > passes a closing and name written on one line
tests/signoff.test.js > findMissingSignoff > reads the sign-off correctly on a body with no unsubscribe footer
tests/signoff.test.js > signoffStyles > drops blanks but keeps the usable entries
tests/signoff.test.js > signoffStyles > returns the user's own closings
tests/signoff.test.js > signoffStyles > survives a missing, empty or malformed list without throwing
tests/supabaseClient.test.js > getSupabaseAdmin > constructs a client without throwing
tests/supabaseClient.test.js > getSupabaseAdmin > memoizes the client across calls
tests/supabaseClient.test.js > getSupabaseAdmin > returns a real client exposing .auth and .from
tests/supabaseClient.test.js > getSupabaseAdmin > throws a clear error when SUPABASE_SERVICE_ROLE_KEY is missing
tests/supabaseClient.test.js > getSupabaseAdmin > throws a clear error when SUPABASE_URL is missing
tests/supabaseClient.test.js > resetSupabaseAdmin > clears the memo so a later call builds a fresh client
tests/threadDetail.test.js > GET /api/threads/:threadId > 400s an implausible thread id with {error} JSON
tests/threadDetail.test.js > GET /api/threads/:threadId > 401s without a token, and never calls Gmail
tests/threadDetail.test.js > GET /api/threads/:threadId > 404s a thread the user did not send from Unique
tests/threadDetail.test.js > GET /api/threads/:threadId > returns the thread for its owner
tests/threadDetail.test.js > getSentThread — authorization > 404s a threadId that is not in this user's send_log, and NEVER calls Gmail
tests/threadDetail.test.js > getSentThread — authorization > 404s another user's threadId — one user cannot read another's mail
tests/threadDetail.test.js > getSentThread — authorization > 404s when the thread has been deleted in Gmail since we sent it
tests/threadDetail.test.js > getSentThread — authorization > requires a userId and a threadId
tests/threadDetail.test.js > getSentThread — content > does NOT apply the ingestion cleaner — the user sees what was really sent
tests/threadDetail.test.js > getSentThread — content > fetches format:full — the detail view needs the actual bodies
tests/threadDetail.test.js > getSentThread — content > logs ids and a count only — never a subject, recipient or body
tests/threadDetail.test.js > getSentThread — content > persists nothing — no insert, update, upsert or delete
tests/threadDetail.test.js > getSentThread — content > returns each message with its headers and plain-text body
tests/threads.test.js > extractAddress > pulls the address out of a display-name header
tests/threads.test.js > listSentThreads — access shape > logs counts only, never subjects or recipients
tests/threads.test.js > listSentThreads — access shape > omits a thread that exists in Gmail but is NOT in send_log
tests/threads.test.js > listSentThreads — access shape > persists nothing — no insert, update, upsert or delete
tests/threads.test.js > listSentThreads — access shape > requires a userId
tests/threads.test.js > listSentThreads — access shape > returns an empty list when nothing has been sent
tests/threads.test.js > listSentThreads — access shape > skips a thread that is in send_log but has been deleted in Gmail
tests/threads.test.js > listSentThreads — access shape > sources thread ids from send_log, never an in:sent search, and never fetches bodies
tests/threads.test.js > listSentThreads — reply detection > does NOT mark a thread replied when every message is from the user
tests/threads.test.js > listSentThreads — reply detection > marks a single-message thread as not replied
tests/threads.test.js > listSentThreads — reply detection > marks a thread replied when a message is NOT from the user
tests/threads.test.js > listSentThreads — reply detection > reports the send time of the first message in the thread
tests/threads.test.js > listSentThreads — search > filters on the recipient, case-insensitively
tests/threads.test.js > listSentThreads — search > filters on the subject, case-insensitively
tests/threads.test.js > listSentThreads — search > never logs the search query
tests/threads.test.js > listSentThreads — search > returns everything for an empty query and nothing for an unmatched one
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > 400s on a token signed with a different secret
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > 400s on an invalid token and writes nothing
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > 400s when the signed payload is missing the user id or email
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > 500s with a generic {error} when the write fails
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > inserts the user id and email carried by the token, on conflict do nothing
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > is public — it records the unsubscribe with no Authorization header
tests/unsubscribe.test.js > POST /api/unsubscribe/:token > never logs the unsubscribed address
tests/unsubscribe.test.js > signToken / verifyToken > produces a URL-safe token that does not expose the payload verbatim
tests/unsubscribe.test.js > signToken / verifyToken > rejects a tampered payload
tests/unsubscribe.test.js > signToken / verifyToken > rejects a tampered signature
tests/unsubscribe.test.js > signToken / verifyToken > rejects a token signed with a different secret
tests/unsubscribe.test.js > signToken / verifyToken > rejects malformed tokens
tests/unsubscribe.test.js > signToken / verifyToken > round-trips a payload
tests/unsubscribeStatus.test.js > POST /api/unsubscribe/:token — outcome status > 400s on an invalid token and writes nothing
tests/unsubscribeStatus.test.js > POST /api/unsubscribe/:token — outcome status > never logs the recipient address, on either outcome
tests/unsubscribeStatus.test.js > POST /api/unsubscribe/:token — outcome status > returns status 'already' on a second call and creates no duplicate row
tests/unsubscribeStatus.test.js > POST /api/unsubscribe/:token — outcome status > returns status 'unsubscribed' on the first call
tests/unsubscribeStatus.test.js > POST /api/unsubscribe/:token — outcome status > treats the same address in different case as already unsubscribed
tests/voice.test.js > buildVoiceProfile > caps the prompt at the 80k char window, keeping the most recent emails
tests/voice.test.js > buildVoiceProfile > makes exactly one Anthropic call with the pinned model
tests/voice.test.js > buildVoiceProfile > rejects an empty inbox without calling the model
tests/voice.test.js > buildVoiceProfile > seeds learned_corrections so the edit-learning loop has somewhere to append
tests/voice.test.js > buildVoiceProfile > surfaces a parse failure without leaking the model output
tests/voice.test.js > capEmailsToCharBudget > defaults to the 80k char window
tests/voice.test.js > capEmailsToCharBudget > ignores empty entries
tests/voice.test.js > capEmailsToCharBudget > keeps everything when it already fits
tests/voice.test.js > capEmailsToCharBudget > keeps the most recent emails that fit the budget
tests/voice.test.js > generateVoiceProfileForUser > ingests, analyses, and saves without ever logging a body
tests/voice.test.js > generateVoiceProfileForUser > rejects when the mailbox has no sent email
tests/voice.test.js > generateVoiceProfileForUser > still saves the profile when exemplar selection fails
tests/voice.test.js > getVoiceProfile > returns null when the user has no profile yet
tests/voice.test.js > getVoiceProfile > returns the row without the encrypted exemplars
tests/voice.test.js > parseExemplarIndices > converts 1-based email numbers to 0-based indices
tests/voice.test.js > parseExemplarIndices > drops out-of-range, duplicate and junk entries
tests/voice.test.js > parseExemplarIndices > returns an empty list for unparseable output
tests/voice.test.js > parseExemplarIndices > strips fences and prose
tests/voice.test.js > parseProfileJson > ignores prose around the object
tests/voice.test.js > parseProfileJson > never echoes the model output in the error
tests/voice.test.js > parseProfileJson > parses a bare JSON object
tests/voice.test.js > parseProfileJson > strips ```json fences
tests/voice.test.js > parseProfileJson > strips bare ``` fences
tests/voice.test.js > parseProfileJson > throws on empty or non-object responses
tests/voice.test.js > parseProfileJson > throws on malformed JSON
tests/voice.test.js > redactPii > leaves ordinary prose and short numbers alone
tests/voice.test.js > redactPii > removes email addresses
tests/voice.test.js > redactPii > removes phone numbers
tests/voice.test.js > redactPii > returns an empty string for non-strings
tests/voice.test.js > saveVoiceProfile > encrypts the exemplars before the write
tests/voice.test.js > saveVoiceProfile > never returns the encrypted exemplars to the caller
tests/voice.test.js > saveVoiceProfile > raises a generic failure when the insert fails
tests/voice.test.js > saveVoiceProfile > stores null when there are no exemplars
tests/voice.test.js > saveVoiceProfile > validates its inputs
tests/voice.test.js > selectExemplars > excludes one-liners from the candidate pool
tests/voice.test.js > selectExemplars > never returns more than 8 anchors
tests/voice.test.js > selectExemplars > returns nothing when there is no usable input
tests/voice.test.js > selectExemplars > returns the emails the model picked, redacted
tests/voice.test.js > selectExemplars > tops up to 5 when the model under-picks

### WEB (/web, vitest, 199 tests) ###
tests/App.test.jsx > App > renders AuthPage when there is no session
tests/App.test.jsx > App > renders OnboardingPage inside the shell when there is a session
tests/App.test.jsx > App > renders UnsubscribePage on /u/<token> with no session, and never AuthPage
tests/AuthPage.test.jsx > AuthPage > disables the submit button while the request is in flight
tests/AuthPage.test.jsx > AuthPage > renders the editorial hero and the sign-in slip
tests/AuthPage.test.jsx > AuthPage > signs in through Supabase in login mode
tests/AuthPage.test.jsx > AuthPage > signs up through Supabase in signup mode
tests/AuthPage.test.jsx > AuthPage > surfaces an auth error
tests/AuthPage.test.jsx > AuthPage > toggles between sign in and sign up
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — mode > shows the template editor and merge bar only in template mode
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — mode > starts in voice mode and switches to template and back
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > blocks submission in template mode until {{personalized}} is present
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > blocks submission without a name
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > goes back to the roll without creating anything
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > posts a template campaign with the template body
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > posts a voice campaign with no template body at all
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — submitting > renders the server message when the create fails, and stays put
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — the merge bar > inserts a merge var at the cursor
tests/CampaignBuilderPage.test.jsx > CampaignBuilderPage — the merge bar > inserts in the middle of the template, not at the end
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — bounce risk is advisory > flags a dead domain without blocking anything, and adds it anyway
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — bounce risk is advisory > reads a lookup that did not finish as unchecked, not as a bad address
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — column mapping > needs no manual mapping for a well-formed file
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — column mapping > re-maps a column by hand and redraws the preview
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — column mapping > shows the preview as the rows will actually be stored, trimmed
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — the email column is the one requirement > blocks the upload until an email column is mapped
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — the email column is the one requirement > counts the rows that have no address instead of hiding them
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — the email column is the one requirement > names an empty file as empty rather than as a missing column
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — uploading > re-bases the row numbers each batch reports, so they point at the file
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — uploading > sends a long file in batches and adds the results up
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — uploading > shows what was added, what was skipped and what was rejected
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — uploading > tells the campaign to reload once the recipients are in
tests/CampaignLeadsPage.test.jsx > CampaignLeadsPage — uploading > tells the user when the server refused the upload
tests/CampaignsPage.test.jsx > CampaignsPage > loads the campaigns on mount
tests/CampaignsPage.test.jsx > CampaignsPage > marks each campaign with its mode
tests/CampaignsPage.test.jsx > CampaignsPage > navigates to a campaign when its row is clicked
tests/CampaignsPage.test.jsx > CampaignsPage > opens the builder from New campaign
tests/CampaignsPage.test.jsx > CampaignsPage > renders a deliberate empty state when there are none
tests/CampaignsPage.test.jsx > CampaignsPage > renders only the campaigns the API returned
tests/CampaignsPage.test.jsx > CampaignsPage > renders the server message when the load fails
tests/CampaignsPage.test.jsx > CampaignsPage > shows each campaign status
tests/CampaignsPage.test.jsx > CampaignsPage > shows the sent and replied counts, zero included
tests/CampaignsPage.test.jsx > CampaignsPage > tallies replies against letters sent across the roll
tests/CampaignsPage.test.jsx > CampaignsPage — one campaign opened > fetches and renders the campaign when one is open
tests/CampaignsPage.test.jsx > CampaignsPage — one campaign opened > goes back to the roll
tests/CampaignsPage.test.jsx > CampaignsPage — one campaign opened > reads a campaign that is not theirs as absent, not as a fault
tests/ComposePage.test.jsx > ComposePage — generating > calls /send/generate and fills the subject and body fields
tests/ComposePage.test.jsx > ComposePage — generating > does not warn when the fidelity score clears the bar
tests/ComposePage.test.jsx > ComposePage — generating > renders a visible warning when the fidelity score is under 80
tests/ComposePage.test.jsx > ComposePage — generating > renders the server error when drafting fails
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > Cancel on the confirm step returns to editing without sending
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > disables "Review & send" until recipient, subject and body are filled
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > hands the confirm dialog exactly what is in the fields
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > lets a hand-written email reach the confirm step without generating
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > never calls /send itself — only the confirm dialog does
tests/ComposePage.test.jsx > ComposePage — reaching the confirmation step > trims the subject before showing it, then sends exactly what it showed
tests/ComposePage.test.jsx > ComposePage — recipient verification > checks the address on blur, not on every keystroke
tests/ComposePage.test.jsx > ComposePage — recipient verification > clears the flag as soon as the address changes
tests/ComposePage.test.jsx > ComposePage — recipient verification > does NOT present the address as bad when the check could not complete
tests/ComposePage.test.jsx > ComposePage — recipient verification > does not re-check an unchanged address on every blur
tests/ComposePage.test.jsx > ComposePage — recipient verification > names a disposable domain specifically
tests/ComposePage.test.jsx > ComposePage — recipient verification > names the specific problem on a warn result
tests/ComposePage.test.jsx > ComposePage — recipient verification > never disables "Review & send" on any verification result — advisory only
tests/ComposePage.test.jsx > ComposePage — recipient verification > re-checks once the address has actually changed
tests/ComposePage.test.jsx > ComposePage — recipient verification > renders a loud red flag for an address that cannot receive mail
tests/ComposePage.test.jsx > ComposePage — recipient verification > renders an understated tick when the domain accepts mail
tests/ComposePage.test.jsx > ComposePage — recipient verification > treats an unreachable server as unknown, never as a bad address
tests/ComposePage.test.jsx > ComposePage — recovering a Gmail grant from the brief > does NOT offer it for an ordinary failure
tests/ComposePage.test.jsx > ComposePage — recovering a Gmail grant from the brief > offers Reconnect Gmail when the server names that recovery
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > does not gate a score of exactly 80
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > lifts the gate on a subject edit too
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > lifts the gate the moment the user edits the body
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > marks the score stale after an edit instead of showing a dead number
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > never gates a hand-written letter — there is no score to gate on
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > opens no confirmation dialog while blocked
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > re-arms the gate when a fresh draft also scores under 80
tests/ComposePage.test.jsx > ComposePage — the fidelity floor > will not let a draft under 80 reach the confirmation step
tests/ComposePage.test.jsx > ComposePage — the goal field grows with what is typed > grows as the brief gets longer and shrinks back when it is cleared
tests/ComposePage.test.jsx > ComposePage — the goal field grows with what is typed > stops growing at the cap and scrolls beyond it
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — cancelling and failures > Cancel sends nothing and hands control back to the caller
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — cancelling and failures > disables the send button while in flight so it cannot double-send
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — cancelling and failures > renders the server error message and re-enables the button
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > Cancel on a follow-up sends nothing
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > adds nothing to the subject of a follow-up — no "Re: " appears here
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > disables the send button while a follow-up is in flight
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > does NOT call /send on render with threading props — only on the Send click
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > lets the caller name the way out after the send, defaulting to "Write another"
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > passes a plain subject through untouched even with the threading props set
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > sends no threadId or inReplyTo for an ordinary first send
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > sends the displayed To, Subject and Body byte-for-byte, plus the thread ids
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — following up inside a thread > still says "Write another" when no label is given
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — recovering a Gmail grant > POSTs /gmail/connect and navigates to the consent URL it returns
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — recovering a Gmail grant > does NOT offer it for an ordinary failure
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — recovering a Gmail grant > keys off the action code, not the wording of the message
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — recovering a Gmail grant > never sends while recovering — the reconnect click posts no message
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — recovering a Gmail grant > offers a Reconnect Gmail button when the server names that recovery
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — the overlay > a click inside the sheet never closes the dialog
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — the overlay > closes on an overlay click while still confirming, and sends nothing
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — the overlay > does NOT close on an overlay click once the letter has been sent
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — what is shown is what is sent > does NOT call /send on render — only after the explicit Send click
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — what is shown is what is sent > includes confirmed: true in the payload
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — what is shown is what is sent > renders the exact To, Subject and Body, including the unsubscribe line
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — what is shown is what is sent > sends the subject and body byte-for-byte as displayed
tests/ConfirmSendDialog.test.jsx > ConfirmSendDialog — what is shown is what is sent > shows the messageId and threadId once the send resolves
tests/CorpusViewer.test.jsx > CorpusViewer — raw text > refetches with includeRaw=1 when the checkbox is ticked
tests/CorpusViewer.test.jsx > CorpusViewer — raw text > shows the body only after expanding the row
tests/CorpusViewer.test.jsx > CorpusViewer — summary > renders the summary numbers
tests/CorpusViewer.test.jsx > CorpusViewer — summary > shows the suspect count
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > does not flag a clean email
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > flags HTML residue in the cleaned text
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > flags a long email where nothing was stripped
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > flags an email long enough to be a whole thread
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > flags an email where no quoted reply was stripped
tests/CorpusViewer.test.jsx > CorpusViewer — suspect detection > flags undecoded quoted-printable markers
tests/CorpusViewer.test.jsx > CorpusViewer — visibility > renders nothing at all when the dev route 404s
tests/OnboardingPage.test.jsx > OnboardingPage — connected > disables the build button while the request is in flight
tests/OnboardingPage.test.jsx > OnboardingPage — connected > posts /voice/generate and shows the building state on click
tests/OnboardingPage.test.jsx > OnboardingPage — connected > renders no email bodies in the ingest stage when the dev preview is absent
tests/OnboardingPage.test.jsx > OnboardingPage — connected > renders the profile summary once the build resolves
tests/OnboardingPage.test.jsx > OnboardingPage — connected > renders the stat strip from /voice/corpus-summary
tests/OnboardingPage.test.jsx > OnboardingPage — connected > shows the connected state and does NOT auto-generate the profile
tests/OnboardingPage.test.jsx > OnboardingPage — disconnected > posts /gmail/connect and navigates to the returned consent url
tests/OnboardingPage.test.jsx > OnboardingPage — disconnected > renders the Connect Gmail button when GET /voice 404s
tests/OnboardingPage.test.jsx > OnboardingPage — errors > renders a failed build as an error and keeps the build button
tests/OnboardingPage.test.jsx > OnboardingPage — errors > renders the server message in an .error element with a retry
tests/OnboardingPage.test.jsx > OnboardingPage — ready > does not crash on a malformed or partial profile_json
tests/OnboardingPage.test.jsx > OnboardingPage — ready > renders a null profile_json without crashing
tests/OnboardingPage.test.jsx > OnboardingPage — ready > renders the dossier including never_does entries
tests/OnboardingPage.test.jsx > OnboardingPage — ready > renders the verbatim clipping lists, the pull-quote and emoji usage
tests/ThreadFollowUp.test.jsx > Follow-up — it goes through the shared confirmation dialog > does NOT call /send when the dialog opens — only the approve click sends
tests/ThreadFollowUp.test.jsx > Follow-up — it goes through the shared confirmation dialog > opens ConfirmSendDialog, not a confirmation of its own
tests/ThreadFollowUp.test.jsx > Follow-up — it goes through the shared confirmation dialog > prefills the recipient from whoever spoke last in the thread
tests/ThreadFollowUp.test.jsx > Follow-up — it goes through the shared confirmation dialog > sends the thread ids, the confirmation, and the body exactly as displayed
tests/ThreadFollowUp.test.jsx > Follow-up — it goes through the shared confirmation dialog > shows the sent confirmation and refreshes that thread
tests/ThreadFollowUp.test.jsx > Follow-up — nothing reaches /send around the dialog > Cancel in the dialog returns to the composer and sends nothing
tests/ThreadFollowUp.test.jsx > Follow-up — nothing reaches /send around the dialog > disables the send button while the follow-up is in flight
tests/ThreadFollowUp.test.jsx > Follow-up — nothing reaches /send around the dialog > posts nothing while browsing, opening and abandoning the composer
tests/ThreadFollowUp.test.jsx > Follow-up — nothing reaches /send around the dialog > will not open the dialog on an empty body, so there is nothing to approve
tests/ThreadFollowUp.test.jsx > Follow-up — the subject shown is the subject sent > does not double-prefix a thread that already reads "Re: "
tests/ThreadFollowUp.test.jsx > Follow-up — the subject shown is the subject sent > leaves an existing prefix in the casing the thread already uses
tests/ThreadFollowUp.test.jsx > Follow-up — the subject shown is the subject sent > shows the "Re: " before the user approves, and sends that exact string
tests/ThreadFollowUp.test.jsx > Follow-up — the subject shown is the subject sent > trims a ragged thread subject before it is read, not after it is approved
tests/ThreadsPage.test.jsx > ThreadsPage > loads sent threads on mount
tests/ThreadsPage.test.jsx > ThreadsPage > makes plain that this is reply detection on sent mail, not an inbox
tests/ThreadsPage.test.jsx > ThreadsPage > polls every 30 seconds and clears the interval on unmount
tests/ThreadsPage.test.jsx > ThreadsPage > renders an empty state when nothing has been sent
tests/ThreadsPage.test.jsx > ThreadsPage > renders only the threads the API returned and invents none of its own
tests/ThreadsPage.test.jsx > ThreadsPage > renders the server message when the load fails
tests/ThreadsPage.test.jsx > ThreadsPage > shows a Replied mark only on threads that were replied to
tests/ThreadsPage.test.jsx > ThreadsPage > tallies the replies against the total in the page head
tests/ThreadsPage.test.jsx > ThreadsPage — refreshing on demand, never under the reader > does NOT refresh the list while a thread is being read
tests/ThreadsPage.test.jsx > ThreadsPage — refreshing on demand, never under the reader > refetches on demand when Refresh is clicked
tests/ThreadsPage.test.jsx > ThreadsPage — refreshing on demand, never under the reader > resumes background refreshes once the reader goes back to the list
tests/ThreadsPage.test.jsx > ThreadsPage — refreshing on demand, never under the reader > says when the register was last updated
tests/ThreadsPage.test.jsx > ThreadsPage — searching the register > debounces the search and calls /threads with the q param
tests/ThreadsPage.test.jsx > ThreadsPage — searching the register > distinguishes an empty search result from having sent nothing
tests/ThreadsPage.test.jsx > ThreadsPage — searching the register > goes back to the unfiltered list when the box is cleared
tests/ThreadsPage.test.jsx > ThreadsPage — searching the register > ignores a stale search response that lands after a newer one
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > distinguishes what was sent from what came back
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > fetches and renders the conversation when a row is clicked
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > goes back to the register from the detail
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > renders a calm not-found when the thread is not one of theirs
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > renders a reply body as text, never as markup
tests/ThreadsPage.test.jsx > ThreadsPage — the thread detail > renders the server message for a real detail failure
tests/UnsubscribePage.test.jsx > UnsubscribePage > posts the token read off the path, on mount
tests/UnsubscribePage.test.jsx > UnsubscribePage > renders a calm invalid state on a 400, with no raw error detail
tests/UnsubscribePage.test.jsx > UnsubscribePage > renders the already-removed state for { status: 'already' } and does not call it an error
tests/UnsubscribePage.test.jsx > UnsubscribePage > renders the invalid state when the path carries no token, without calling the API
tests/UnsubscribePage.test.jsx > UnsubscribePage > renders the removed state for { status: 'unsubscribed' }
tests/csv.test.js > decodeCsvFile — encoding > accepts an ArrayBuffer as well as a byte view, and an empty file as empty text
tests/csv.test.js > decodeCsvFile — encoding > decodes a windows-1252 file to the right characters rather than mojibake
tests/csv.test.js > decodeCsvFile — encoding > does not mistake valid multi-byte UTF-8 for latin-1
tests/csv.test.js > decodeCsvFile — the byte-order mark > takes the BOM off before the windows-1252 fallback, where it would become mojibake
tests/csv.test.js > decodeCsvFile — the byte-order mark > takes the mark off at the BYTE level, so it is gone whichever decoder then runs
tests/csv.test.js > guessMapping > maps a header padded with spaces
tests/csv.test.js > guessMapping > maps by name, so the column order makes no difference
tests/csv.test.js > guessMapping > never gives one column to two fields
tests/csv.test.js > guessMapping > recognises the names the same columns are given in the wild
tests/csv.test.js > guessMapping > reports no email column when the file simply has not got one
tests/csv.test.js > normaliseHeader > reduces one column name written every which way to a single key
tests/csv.test.js > parseCsv — files with nothing in them > says a headings-only file has no rows, which is a different problem
tests/csv.test.js > parseCsv — files with nothing in them > says an empty file is empty
tests/csv.test.js > parseCsv — rows that do not agree with the heading row > keeps commas and newlines that live inside a quoted field
tests/csv.test.js > parseCsv — rows that do not agree with the heading row > reports a row with too few fields and still keeps the rest of the file
tests/csv.test.js > parseCsv — rows that do not agree with the heading row > reports a row with too many fields and drops only the overflow
tests/csv.test.js > parseCsv — rows that do not agree with the heading row > reports an unbalanced quote instead of throwing on it
tests/csv.test.js > partitionRows > copes with nothing at all
tests/csv.test.js > partitionRows > separates the rows with no email rather than dropping them
tests/csv.test.js > toRows > follows a column re-mapped by hand rather than the guess
tests/csv.test.js > toRows > ignores the columns nobody mapped
tests/csv.test.js > toRows > trims every value it takes
tests/subject.test.js > normaliseSubject > leaves an already-clean subject byte-for-byte alone
tests/subject.test.js > normaliseSubject > treats whitespace-only and non-strings as no subject at all
tests/subject.test.js > normaliseSubject > trims the subject the user typed
tests/subject.test.js > withReplyPrefix > does not mistake a word starting with "re" for the prefix
tests/subject.test.js > withReplyPrefix > is idempotent — applying it twice changes nothing
tests/subject.test.js > withReplyPrefix > leaves an already-prefixed subject alone, whatever its casing
tests/subject.test.js > withReplyPrefix > never invents a bare "Re:" out of an empty subject
tests/subject.test.js > withReplyPrefix > prefixes a plain thread subject once
tests/subject.test.js > withReplyPrefix > trims before prefixing, so the result carries no stray whitespace
